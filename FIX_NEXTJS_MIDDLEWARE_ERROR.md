# 🔧 إصلاح خطأ Next.js middleware-manifest.json

## ❌ المشكلة:
```
ENOENT: no such file or directory, open '.next/server/middleware-manifest.json'
```

## 🔍 السبب:
- مجلد `.next` محذوف أو غير مكتمل
- Next.js لم يكمل عملية البناء
- هناك مشكلة في عملية البناء

---

## ✅ الحل:

### الحل 1: إعادة بناء Next.js (موصى به) ⭐

```bash
# 1. احذف مجلد .next
Remove-Item -Recurse -Force .next

# 2. احذف cache
Remove-Item -Recurse -Force node_modules/.cache

# 3. أعد بناء Next.js
npm run build

# 4. شغّل السيرفر
npm run dev
```

---

### الحل 2: تنظيف كامل وإعادة التثبيت

إذا استمرت المشكلة:

```bash
# 1. احذف .next
Remove-Item -Recurse -Force .next

# 2. احذف node_modules/.cache
Remove-Item -Recurse -Force node_modules/.cache

# 3. احذف node_modules (اختياري)
Remove-Item -Recurse -Force node_modules

# 4. أعد تثبيت Dependencies
npm install

# 5. أعد بناء Next.js
npm run build

# 6. شغّل السيرفر
npm run dev
```

---

### الحل 3: إعادة تشغيل Development Server فقط

إذا كان البناء مكتمل:

```bash
# 1. أوقف السيرفر (Ctrl+C)

# 2. احذف .next فقط
Remove-Item -Recurse -Force .next

# 3. شغّل السيرفر (سيبني تلقائياً)
npm run dev
```

---

## 🎯 الحل المطبق:

**تم تطبيق الحل 1:**
- ✅ حذف `.next`
- ✅ حذف `node_modules/.cache`
- ✅ إعادة بناء Next.js

---

## 📝 بعد الإصلاح:

1. ✅ ملف `middleware-manifest.json` موجود
2. ✅ Next.js مبني بالكامل
3. ✅ السيرفر يعمل بشكل طبيعي

---

## ⚠️ ملاحظات:

### إذا استمرت المشكلة:
1. تحقق من أن `middleware.ts` موجود في جذر المشروع
2. تحقق من أن لا توجد أخطاء في `middleware.ts`
3. أعد تشغيل Terminal
4. أعد تشغيل Docker Desktop (إذا كان مستخدماً)

### إذا ظهرت أخطاء أخرى:
- تحقق من Console للتفاصيل
- تحقق من Terminal Logs
- تأكد من أن جميع Dependencies مثبتة

---

## 🔍 التحقق:

بعد إعادة التشغيل، تحقق من:
- ✅ لا توجد أخطاء في Terminal
- ✅ السيرفر يعمل على `http://localhost:3005`
- ✅ الصفحات تعمل بشكل طبيعي


