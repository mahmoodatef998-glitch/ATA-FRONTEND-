# ⚡ إعداد سريع لـ RBAC

## 🚀 خطوات سريعة (دقيقة واحدة)

### 1. افتح ملف `.env` في جذر المشروع

### 2. أضف هذه الأسطر في نهاية الملف:

```env
RBAC_ENABLED=true
NEXT_PUBLIC_RBAC_ENABLED=true
```

### 3. احفظ الملف

### 4. أعد تشغيل Development Server:
```bash
# أوقف السيرفر (Ctrl+C)
# ثم شغله مرة أخرى
npm run dev
```

---

## ✅ التحقق من الإعداد

بعد إعادة التشغيل، ابحث في Console عن:
```
🔐 RBAC Configuration: {
  RBAC_ENABLED: true,
  ...
}
```

إذا رأيت هذه الرسالة، فالإعداد صحيح! ✅

---

## ⚠️ إذا لم تضيف هذه القيم:

- ❌ النظام سيستخدم النظام القديم
- ❌ RBAC لن يعمل
- ❌ الصلاحيات قد لا تعمل بشكل صحيح

---

## 📋 مثال كامل:

```env
# ... باقي الإعدادات الموجودة ...

# RBAC System
RBAC_ENABLED=true
NEXT_PUBLIC_RBAC_ENABLED=true
```

---

## ✅ بعد الإعداد

النظام جاهز! 🎉


