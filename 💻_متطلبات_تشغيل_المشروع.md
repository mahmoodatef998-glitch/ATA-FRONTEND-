# 💻 متطلبات تشغيل المشروع - ATA CRM

## ✅ **قائمة البرامج المطلوبة على اللابتوب الجديد**

---

## 1️⃣ **Node.js (إجباري)** 🟢

### **التحميل:**
```
الموقع: https://nodejs.org/
النسخة: LTS (Long Term Support)
الحالية: v20.x أو v18.x
```

### **التثبيت:**
```
1. Download Node.js LTS
2. Run installer
3. Accept defaults
4. ✅ Restart terminal
```

### **التحقق:**
```bash
node --version
# Should show: v20.x.x or v18.x.x

npm --version
# Should show: 10.x.x or 9.x.x
```

---

## 2️⃣ **Docker Desktop (إجباري)** 🐳

### **التحميل:**
```
الموقع: https://www.docker.com/products/docker-desktop/
نسخة Windows: Docker Desktop for Windows
```

### **التثبيت:**
```
1. Download Docker Desktop
2. Run installer
3. Enable WSL 2 (Windows Subsystem for Linux) إذا طُلب
4. Restart computer
5. Start Docker Desktop
6. ✅ Wait for Docker to start
```

### **التحقق:**
```bash
docker --version
# Should show: Docker version 24.x.x or higher

docker-compose --version
# Should show: Docker Compose version 2.x.x
```

### **مهم:**
```
⚠️ Docker Desktop لازم يكون شغال دائماً!
   → يشغّل PostgreSQL database
   → بدونه المشروع مش هيشتغل
```

---

## 3️⃣ **Git (اختياري لكن مفيد)** 📦

### **التحميل:**
```
الموقع: https://git-scm.com/download/win
النسخة: Latest
```

### **التثبيت:**
```
1. Download Git for Windows
2. Run installer
3. Accept defaults
4. ✅ Restart terminal
```

### **التحقق:**
```bash
git --version
# Should show: git version 2.x.x
```

---

## 4️⃣ **Code Editor (اختياري)** 📝

### **الخيارات:**

#### **VS Code (Recommended):**
```
الموقع: https://code.visualstudio.com/
مجاني: ✅
سهل: ✅

Extensions المفيدة:
├── Prisma
├── Tailwind CSS IntelliSense
├── ES7+ React/Redux snippets
└── Pretty TypeScript Errors
```

#### **أو Cursor (AI-powered):**
```
الموقع: https://cursor.sh/
مجاني: ✅ (مع حدود)
AI Features: ✅
```

---

## ✅ **الخلاصة - البرامج الإجبارية:**

```
1. Node.js (v18+ أو v20+)          ✅ إجباري
2. Docker Desktop                   ✅ إجباري
3. Git                             ⚪ اختياري
4. VS Code / Cursor                ⚪ اختياري
```

---

## 🚀 **خطوات التشغيل بعد التثبيت:**

### **Step 1: Open Terminal في مجلد المشروع**
```bash
cd "C:\path\to\ATA CRM PROJECT"
```

---

### **Step 2: Install Node Packages**
```bash
npm install
```
**هينزّل:**
- ✅ Next.js
- ✅ React
- ✅ Prisma
- ✅ TypeScript
- ✅ Tailwind CSS
- ✅ كل الـ dependencies (300+ package)

**الوقت:** ~5-10 دقائق (حسب النت)

---

### **Step 3: Setup Environment Variables**
```bash
# انسخ ENV_TEMPLATE.txt إلى .env
copy ENV_TEMPLATE.txt .env

# ثم افتح .env وعدّل:
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ata_crm_db"
NEXTAUTH_URL="http://localhost:3005"
NEXTAUTH_SECRET="your-random-secret-here"
EMAIL_USER="your-email@gmail.com"
EMAIL_PASS="your-app-password"
```

---

### **Step 4: Start Docker Desktop**
```
1. افتح Docker Desktop
2. انتظر حتى يكتمل التشغيل (Docker icon يصبح green)
3. ✅ Docker ready
```

---

### **Step 5: Run Project**
```bash
START.bat
```
**هيعمل:**
- ✅ يشغّل PostgreSQL container
- ✅ يطبق Prisma migrations
- ✅ يبدأ Next.js server (port 3005)
- ✅ يفتح Prisma Studio (port 5556)

---

### **Step 6: Open Browser**
```
Admin: http://localhost:3005/login
Client: http://localhost:3005/client/login

Login:
Email: admin@ata.com
Password: admin123
```

---

## 🔧 **Troubleshooting:**

### **مشكلة: "node is not recognized"**
```
الحل:
1. تأكد من تثبيت Node.js
2. Restart terminal
3. أو Restart computer
```

---

### **مشكلة: "docker is not recognized"**
```
الحل:
1. تأكد من تثبيت Docker Desktop
2. شغّل Docker Desktop
3. انتظر حتى يكتمل التشغيل
4. Restart terminal
```

---

### **مشكلة: "Port 3005 already in use"**
```
الحل:
1. STOP.bat (لإيقاف كل العمليات)
2. ثم START.bat مرة أخرى
```

---

### **مشكلة: "Cannot connect to database"**
```
الحل:
1. تأكد Docker Desktop شغال
2. انتظر 10 ثواني
3. START.bat مرة أخرى
```

---

### **مشكلة: "Module not found"**
```
الحل:
1. امسح node_modules/
2. npm install
3. انتظر التحميل
4. START.bat
```

---

## 📋 **Quick Checklist:**

```
قبل التشغيل، تأكد من:

□ Node.js مثبّت (v18+ أو v20+)
□ Docker Desktop مثبّت
□ Docker Desktop شغال (icon أخضر)
□ npm install تم تنفيذه
□ .env file موجود ومُعدّل
□ Terminal مفتوح في مجلد المشروع
□ Port 3005 فاضي
□ Port 5432 فاضي (للـ PostgreSQL)

✅ كل حاجة تمام → START.bat
```

---

## ⏱️ **الوقت المتوقع:**

```
تحميل البرامج:
├── Node.js: ~2 دقيقة
├── Docker Desktop: ~5 دقائق
└── Total: ~7 دقائق

Setup المشروع:
├── npm install: ~5-10 دقائق
├── Docker pull PostgreSQL: ~2 دقيقة
├── Prisma setup: ~1 دقيقة
└── Total: ~8-13 دقيقة

الإجمالي: ~15-20 دقيقة
```

---

## 🎯 **Summary:**

### **البرامج الأساسية (2):**
```
1. Node.js v20.x LTS
   → https://nodejs.org/
   → Required for Next.js

2. Docker Desktop
   → https://www.docker.com/
   → Required for PostgreSQL
```

### **البرامج الاختيارية:**
```
3. Git (للـ version control)
4. VS Code (للتعديل)
```

---

## 🎊 **بعد التثبيت:**

```bash
# 1. Install packages
npm install

# 2. Setup .env
copy ENV_TEMPLATE.txt .env
# ثم عدّل .env

# 3. Start everything
START.bat

# 4. Open browser
http://localhost:3005

# 5. Login
admin@ata.com / admin123

# 6. ✅ Enjoy!
```

---

## 📞 **لو واجهت مشاكل:**

```
1. تأكد من Node.js و Docker مثبتين
2. تأكد Docker Desktop شغال
3. Run: npm install
4. Run: STOP.bat ثم START.bat
5. لو المشكلة مستمرة → راجع 🧹_تنظيف_المشروع_ملخص.md
```

---

**حمّل البرامج دي وهتكون جاهز! 💻✨**

